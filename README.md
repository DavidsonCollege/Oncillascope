# Oncillascope update feed

This branch hosts the Sparkle appcast for Oncillascope, published automatically by
`.github/workflows/release.yml` on each `v*.*.*` tag. `appcast.xml` appears here after
the first release. See `RELEASING.md` in the default branch.
